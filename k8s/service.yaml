apiVersion: v1
kind: Service
metadata:
  name: hyper-web-service
  namespace: hyper-production
  labels:
    app: hyper-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hyper-web
